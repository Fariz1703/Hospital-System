<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_department_tree" model="ir.ui.view">
        <field name="name">hospital_system.department.tree</field>
        <field name="model">hospital_system.department</field>
        <field name="arch" type="xml">
            <tree string="Departments">
                <field name="department_id"/>
                <field name="department_name"/>
                <field name="department_type"/>
            </tree>
        </field>
    </record>


    <record id="view_department_form" model="ir.ui.view">
        <field name="name">hospital_system.department.form</field>
        <field name="model">hospital_system.department</field>
        <field name="arch" type="xml">
            <form string="Department">
                <sheet>

                    <group>
                        <field name="department_id" readonly="1"/>
                        <field name="department_name"/>
                        <field name="department_type"/>
                    </group>

                </sheet>
            </form>
        </field>
    </record>


    <record id="view_department_search" model="ir.ui.view">
        <field name="name">hospital_system.department.search</field>
        <field name="model">hospital_system.department</field>
        <field name="arch" type="xml">

            <search>

                <field name="department_id"/>
                <field name="department_name"/>
                <field name="department_type"/>

                <group expand="0" string="Group By">

                    <filter
                        name="group_type"
                        string="Department Type"
                        context="{'group_by':'department_type'}"
                    />

                </group>

            </search>

        </field>
    </record>


    <record id="action_department" model="ir.actions.act_window">
        <field name="name">Departments</field>
        <field name="res_model">hospital_system.department</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_department_search"/>
    </record>

</odoo>