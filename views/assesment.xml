<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ================= TREE VIEW ================= -->

    <record id="view_hospital_assessment_tree" model="ir.ui.view">
        <field name="name">hospital.assessment.tree</field>
        <field name="model">hospital_system.assessment</field>
        <field name="arch" type="xml">

            <tree string="Assessments">
                <field name="assessment_id"/>
                <field name="appendix_number"/>
                <field name="appointment_id"/>
                <field name="patient_id"/>
                <field name="doctor_id"/>
                <field name="assessment_date"/>
                <field name="state"/>
            </tree>

        </field>
    </record>


    <!-- ================= FORM VIEW ================= -->

    <record id="view_hospital_assessment_form" model="ir.ui.view">
        <field name="name">hospital.assessment.form</field>
        <field name="model">hospital_system.assessment</field>
        <field name="arch" type="xml">

            <form string="Doctor Assessment">

                <header>

                    <button name="action_start"
                            type="object"
                            string="Start Assessment"
                            class="btn-primary"
                            invisible="state != 'draft'"/>

                    <button name="action_done"
                            type="object"
                            string="Complete"
                            class="btn-success"
                            invisible="state != 'in_progress'"/>

                    <button name="action_cancel"
                            type="object"
                            string="Cancel"
                            class="btn-danger"
                            invisible="state in ('done','cancel')"/>

                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="draft,in_progress,done"/>

                </header>


                <sheet>

                    <group>
                        <group>
                            <field name="assessment_id" readonly="1"/>
                            <field name="appointment_id"/>
                            <field name="assessment_date"/>
                        </group>

                        <group>
                            <field name="patient_id" readonly="1"/>
                            <field name="doctor_id" readonly="1"/>
                        </group>
                    </group>


                    <!-- ================= VITAL SIGNS ================= -->

                    <group string="Vital Signs">

                        <group>
                            <field name="temperature"/>
                            <field name="blood_pressure"/>
                            <field name="heart_rate"/>
                        </group>

                        <group>
                            <field name="respiratory_rate"/>
                            <field name="weight"/>
                            <field name="height"/>
                        </group>

                    </group>


                    <!-- ================= MEDICAL NOTES ================= -->

                    <group string="Medical Assessment">

                        <field name="chief_complaint"/>

                        <field name="diagnosis"/>

                        <field name="treatment_plan"/>

                        <field name="notes"/>

                    </group>

                    <field name="service_line_ids">
                        <tree editable="bottom">
                            <field name="service_id"/>
                            <field name="quantity"/>
                            <field name="price"/>
                            <field name="subtotal"/>
                        </tree>
                    </field>
                </sheet>


                <!-- ================= OE CHATTER ================= -->

                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>


            </form>

        </field>
    </record>

    <!-- ================= ACTION ================= -->

    <record id="action_hospital_assessment" model="ir.actions.act_window">
        <field name="name">Doctor Assessments</field>
        <field name="res_model">hospital_system.assessment</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>